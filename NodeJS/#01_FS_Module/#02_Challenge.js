// Challenge

const fs = require("fs");

// 1. Create a folder named thapa
// fs.mkdirSync("thapa");

// 2. Create a file in it named bio.txt and data into it.
// fs.writeFileSync(
//   "./thapa/mybio.txt",
//   "this the data data to the file during th challnege"
// );

// 3. Add more data into the file at the end of the existing data.
// fs.appendFileSync(
//   "./thapa/mybio.txt",
//   "\n this is the new data sppended after the alreadyex isting data and in another line"
// );

// 4. read the data without getting the buffer data first.  (FILE ENCODING)
// const data = fs.readFileSync("./thapa/mybio.txt", "utf-8");
// console.log(data);

// 5. Rename the file name to mybio.txt
// fs.renameSync("./thapa/mybio.txt", "./thapa/file.txt");

// 6. now delete both the file and the folder.
// to remove the file
// fs.unlinkSync("./thapa/file.txt");

// to remove the folder
// fs.rmdirSync("thapa");
